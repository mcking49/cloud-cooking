import { ComponentStyleConfig } from '@chakra-ui/react'

const Form: ComponentStyleConfig = {
  baseStyle: {
    width: 'full',
  },
  variants: {},
  defaultProps: {
    variant: '',
  },
}

export default Form
